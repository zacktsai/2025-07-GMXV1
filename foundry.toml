# ===== 安全审计专用配置 =====
[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.19"

# 🚨 审计关键：关闭优化器
optimizer = false
optimizer_runs = 1        # 即使启用也设为最小值
via_ir = false           # 关闭 IR 优化
bytecode_hash = "none"   # 确保字节码一致性
cbor_metadata = false    # 移除元数据干扰

# 🚀 真实攻击配置：高 Gas Limit
gas_limit = 50000000     # 50M Gas - 足够真实大规模攻击

# 测试配置 - 审计友好
fuzz = { runs = 1000 }   # 增加模糊测试次数
gas_reports = ["*"]      # 详细的 gas 报告
verbosity = 3            # 详细日志

# 路径映射
remappings = [
    "forge-std/=lib/forge-std/src/",
    "openzeppelin-contracts/=lib/openzeppelin-contracts/contracts/"
]

# ===== 生产部署配置（对比用）=====
[profile.production]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.19"
optimizer = true         # 生产环境才启用优化
optimizer_runs = 200
via_ir = false
gas_limit = 30000000     # 生产环境较低的 gas limit

# ===== CI/CD 配置 =====
[profile.ci]
fuzz = { runs = 10_000 }
verbosity = 4
optimizer = false        # CI 中也关闭优化以确保一致性
gas_limit = 20000000     # CI 环境中等 gas limit

# ===== 审计深度测试配置 =====
[profile.audit]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.19"
optimizer = false        # 审计必须关闭
optimizer_runs = 1
via_ir = false
fuzz = { runs = 50_000 }  # 超高强度模糊测试
gas_reports = ["*"]
verbosity = 4
gas_limit = 100000000    # 100M Gas - 极限测试

# 强制显示所有警告
extra_output = ["storageLayout", "devdoc", "userdoc"]
ignored_error_codes = []  # 不忽略任何警告

[fmt]
bracket_spacing = true
int_types = "long"
line_length = 120
multiline_func_header = "all"
number_underscore = "thousands"
quote_style = "double"
tab_width = 4
wrap_comments = true